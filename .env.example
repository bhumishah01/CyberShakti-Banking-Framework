# Copy to ".env" for local runs (DO NOT commit real secrets).

# Server API (Postgres + JWT)
DATABASE_URL=postgresql+psycopg://ruralshield:ruralshield_dev@localhost:5432/ruralshield
JWT_SECRET=dev-change-me
FIELD_ENC_KEY=dev-change-me-to-32chars-minimum

# Optional: if you deploy behind a proxy
# PORT=8000

